#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export STACK_APP_ROOT="${STACK_APP_ROOT:-$SCRIPT_DIR}"
exec bun run "$SCRIPT_DIR/src/mcp/server.ts" "$@"

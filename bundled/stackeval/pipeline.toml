# StackEval pipeline defaults (bundled; seeded into .stack/stackeval on first run)
# Consumed by: stack/bin/stackeval -> scripts/stackeval/pipeline.sh

[paths]
stack_root = "."
workspace_root = "."
gepa_evals_project = "${GEPA_EVALS_PROJECT}"
gepa_cookbook_root = "${GEPA_COOKBOOK_ROOT}"
synth_ai_env = "${SYNTH_AI_ENV:-}"
trace_root = ".stack/evidence/stackeval"

[pipeline]
default_preset = "smoke"
stages = ["prepare", "preflight", "harness", "harvest", "export", "grade", "review", "finalize"]

[stack]
default_model = "gpt-5.5-low"
autosubmit = true
stack_api_url = "http://127.0.0.1:8792"

[grader]
model = "gpt-5.3-codex-spark"
prompt = ".stack/stackeval/grader_prompt.md"
skip_git_repo_check = true

[reviewer]
enabled = true
model = "gpt-5.3-codex-spark"
prompt = ".stack/stackeval/reviewer_prompt.md"
skip_git_repo_check = true

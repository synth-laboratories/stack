## Summary

## Test plan

- [ ] `bun run check`
- [ ] `bun run scripts/check_version_drift.ts`

## Notes

- [ ] No version hand-edits in `package.json` / `Cargo.toml` (only `version.json` + `make sync-version` if needed)
- [ ] No secrets in diff

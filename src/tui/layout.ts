/** Stack TUI chrome density. Terminal glyph size is emulator-controlled — see `.vscode/settings.json`. */
export const stackTuiLayout = {
  rootPadding: 0,
  rootGap: 0,
  panelPadding: 0,
  panelGap: 0,
  controlGap: 0,
} as const

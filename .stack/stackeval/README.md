# StackEval

StackEval task configs live in this directory and are owned by Stack.

## Active task id

There is **one** registered StackEval task:

| Task id | Command |
| --- | --- |
| **`banking77-local-gepa`** | `./bin/stackeval run banking77-local-gepa --preset smoke` |

Presets: `smoke` (plumbing), `dev` (optimization signal), `gate` (heldout lift required).

Task prompts and task metadata live under `tasks/banking77-local-gepa.*`.
